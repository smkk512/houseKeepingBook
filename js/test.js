
let list = new Array(10);

const RandomList = _list =>{
    let randList = [];
    
    for(i=0;i<_list.length;i++){
        _list[i] = i;
    }
    
    
    
}

const QuickSort = () => {
    
}

const init = () => {
    RandomList(list);
    console.log(list);
}

init();